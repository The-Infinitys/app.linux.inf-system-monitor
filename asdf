#include <iostream>
#include <cstdio>

void drawAtPosition(int x, int y, const std::string& text);

int main() {
    // ターミナルをクリア

    drawAtPosition(0, 0, "0");
    drawAtPosition(5, 2, "Hello, World!");
    drawAtPosition(10, 4, "ABC");
    
    std::cout << "\033[20;0H";
    return 0;
}
